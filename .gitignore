node_modules
dist
graft/.cache
playwright-report
test-results
*.local
.DS_Store
.agents/
.codex/
# Content pipeline handoff artifacts — the shared Google Spreadsheet is the
# editing surface; data.json + src/data/ui.json are canonical in-repo.
content/
content.config.json

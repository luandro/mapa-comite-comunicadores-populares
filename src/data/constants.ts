/** Shared scene constants — single source for schema validation AND the calibration tool. */
export const POS_MARGIN = 0
export const SCENE_WIDTH = 3023.11
export const SCENE_HEIGHT = 2021.19

export const SCENE_RECT = Object.freeze({
  x: 0,
  y: 0,
  width: SCENE_WIDTH,
  height: SCENE_HEIGHT,
})
